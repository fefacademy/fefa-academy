const CourseIndex = () => {
  // A hero image with course title, intro, author and all other metadata
  // Burndown of pre-requisites
  // download options: direct, torrent, rclone
  // course sections
  // course overview
  // reviews
  // authors info
  // course tags
  // projects images, what you'll build
  // resources
  // get the course cert
  return <div>CourseIndex</div>;
};

export default CourseIndex;
